<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ================= STARK permissions =================
         Each permission maps to a real Stark capability —
         nothing speculative (contract §48).                    -->

    <!-- Dio REST calls to the Go API; Paystack authorization pages -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- connectivity_plus — Stark Turbo diagnostics / offline banner -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Profile photo capture + QR receipt scanning (point-of-use request) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- local_auth — fingerprint / face / device-credential unlock.
         Biometric data never leaves the device. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <!-- FCM transaction & security alerts (Android 13+, runtime-requested) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="STARK"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text (Flutter template). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
